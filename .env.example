# MarketCheck dashboard API Key only — not the client secret.
# Copy to .env.local for local / cloud-agent tests, then:
#   npm run check:marketcheck
MARKETCHECK_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/auto

